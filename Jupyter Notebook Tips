To install new packages in jupyter notebook, use command:

#For pip
import sys
!{sys.executable} -m pip install <package name>

#For conda
import sys
!conda install --yes --prefix {sys.prefix} <package name>
